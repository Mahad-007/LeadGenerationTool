# Backend Configuration

# CORS origins (comma-separated for multiple)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Project root (optional, defaults to parent directory)
# PROJECT_ROOT=/path/to/LeadGen
